/**
 * renderContract.ts
 *
 * Backward-compatible alias for the centralized render contracts used by telemetry
 * and render verification. Prefer importing from renderContracts.ts directly.
 */

export { RENDER_CONTRACTS as RENDER_CONTRACT } from "./renderContracts.js";

